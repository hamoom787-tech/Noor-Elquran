# 🚀 دليل البدء السريع - Quick Start Guide

## خطوات التشغيل السريعة

### 1️⃣ تثبيت المكتبات
افتح PowerShell أو Command Prompt في مجلد المشروع وقم بتشغيل:
```bash
pip install -r requirements.txt
```

### 2️⃣ إضافة فيديوهات الخلفية
- ضع على الأقل ملف فيديو واحد (.mp4) في مجلد `vision/`
- يمكنك تحميل فيديوهات مجانية من:
  - **Pixabay**: https://pixabay.com/videos/
  - ابحث عن: "nature vertical" أو "peaceful background"

### 3️⃣ التحقق من FFmpeg
قم بتشغيل الأمر التالي للتحقق من تثبيت FFmpeg:
```bash
ffmpeg -version
```

**إذا لم يكن مثبتاً:**
- Windows: حمّل من https://ffmpeg.org/download.html
- أو استخدم: `choco install ffmpeg` (إذا كان Chocolatey مثبتاً)

### 4️⃣ تشغيل التطبيق
```bash
python main.py
```

### 5️⃣ فتح المتصفح
افتح المتصفح على:
```
http://localhost:5000
```

---

## ✅ قائمة التحقق قبل التشغيل

- [ ] Python 3.8+ مثبت
- [ ] المكتبات المطلوبة مثبتة (`pip install -r requirements.txt`)
- [ ] FFmpeg مثبت ومتاح في PATH
- [ ] ملف فيديو واحد على الأقل في مجلد `vision/`
- [ ] الاتصال بالإنترنت متاح (لأول مرة فقط لجلب البيانات)

---

## 🎬 أول فيديو تجريبي

بعد فتح الواجهة:

1. **اختر القارئ**: مثل "عبد الباسط عبد الصمد"
2. **اختر السورة**: سورة الفاتحة (1)
3. **من الآية**: 1
4. **إلى الآية**: 1
5. **انقر**: "إنشاء الفيديو"

---

## 💡 نصائح

### للحصول على أفضل النتائج:
- استخدم فيديوهات خلفية بدقة 1080x1920 (عمودي)
- اختر آيات قصيرة في البداية (1-3 آيات)
- تأكد من أن الفيديو الخلفي هادئ وغير مزدحم

### إذا واجهت مشاكل:
- تحقق من مجلد `outputs/` للفيديوهات المولدة
- راجع سجل الأخطاء في نافذة الـ terminal
- تأكد من أن المنفذ 5000 غير مستخدم من تطبيق آخر

---

## 📂 الملفات المهمة

| الملف | الوصف |
|------|------|
| `main.py` | السيرفر الخلفي (Backend) |
| `UI.html` | واجهة المستخدم (Frontend) |
| `config.py` | إعدادات التطبيق |
| `vision/` | مجلد فيديوهات الخلفية |
| `outputs/` | الفيديوهات النهائية المولدة |

---

## 🆘 حل المشاكل الشائعة

### المشكلة: "FFmpeg not found"
**الحل**: 
1. ثبت FFmpeg من الموقع الرسمي
2. أضفه إلى PATH
3. أو حدد المسار الكامل في `config.py`

### المشكلة: "لا توجد فيديوهات خلفية"
**الحل**: ضع ملف .mp4 في مجلد `vision/`

### المشكلة: "Port 5000 already in use"
**الحل**: أغلق التطبيق الذي يستخدم المنفذ 5000 أو عدّل المنفذ في `main.py` (السطر الأخير)

---

**مستعد للبدء؟ شغّل `python main.py` الآن! 🚀**
